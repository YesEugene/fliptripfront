# 🔧 Расширенное решение проблемы с фотографиями (403 Forbidden)

## Если Places API (New) уже включен, но фото все равно не работают

### Проблема
Фотографии загружаются через URL вида:
```
https://maps.googleapis.com/maps/api/place/photo?maxwidth=800&photoreference=...&key=...
```

Когда браузер загружает это изображение, Google проверяет **HTTP referrer** (откуда идет запрос). Если домен не в списке разрешенных, возвращается `403 Forbidden`.

### Решение 1: Проверить все домены в Application Restrictions

1. Откройте консоль браузера (F12) → вкладка **Network**
2. Найдите запрос к `/place/photo` с ошибкой 403
3. Посмотрите в заголовках запроса - какой **Referer** указан
4. Убедитесь, что этот домен добавлен в **Application Restrictions**

**Примеры доменов, которые могут быть нужны:**
- `flip-trip.com/*`
- `www.flip-trip.com/*`
- `fliptripfrontend.vercel.app/*`
- `*.fliptripfrontend.vercel.app/*` (для всех поддоменов)
- `*.vercel.app/*` (для preview deployments)
- `localhost:5173/*` (для разработки)

### Решение 2: Использовать прокси на бэкенде (если не помогает)

Если ограничения по доменам не работают, можно загружать фото через бэкенд:

1. Создать endpoint на бэкенде для проксирования фото
2. Фронтенд загружает фото через этот endpoint
3. Бэкенд использует серверный ключ (без ограничений по доменам)

### Решение 3: Проверить, что ключ правильный

1. Убедитесь, что в **API restrictions** включены:
   - ✅ Maps JavaScript API
   - ✅ Places API
   - ✅ Places API (New)
   - ✅ Geocoding API

2. Проверьте, что ключ не истек и не заблокирован

### Решение 4: Временное решение - убрать ограничения по доменам

Если ничего не помогает, можно временно:
1. В **Application restrictions** выбрать **"Don't restrict"**
2. Проверить, работают ли фото
3. Если работают - значит проблема в ограничениях по доменам
4. Затем постепенно добавлять домены обратно

**⚠️ ВНИМАНИЕ:** Не оставляйте ключ без ограничений в продакшене!

### Диагностика

1. Откройте консоль браузера (F12)
2. Перейдите на вкладку **Network**
3. Найдите запрос к `maps.googleapis.com/maps/api/place/photo`
4. Посмотрите:
   - **Status:** должен быть 200, не 403
   - **Request Headers:** какой Referer отправляется
   - **Response:** что возвращает Google

### Частые ошибки

- **403 Forbidden:** Домен не в списке разрешенных
- **400 Bad Request:** Неправильный формат URL или ключ
- **401 Unauthorized:** Ключ недействителен или истек




