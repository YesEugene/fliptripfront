# 🔧 Исправление 403 ошибок для фотографий локаций

## Проблема
Изображения в блоках локаций не загружаются, в консоли появляются ошибки `403 Forbidden` для запросов к `maps.googleapis.com/maps/api/place/photo`.

## Причина
Google проверяет **HTTP referrer** при загрузке изображений. Если домен не добавлен в **Application Restrictions**, возвращается 403.

## Решение

### 1. Проверить, какой домен используется

1. Откройте консоль браузера (F12) → вкладка **Network**
2. Найдите запрос к `/place/photo` с ошибкой 403
3. Посмотрите в **Request Headers** → **Referer**
4. Запомните этот домен

### 2. Добавить домен в Application Restrictions

1. Google Cloud Console → **APIs & Services** → **Credentials**
2. Откройте ваш API ключ
3. В разделе **"Application restrictions"** → **"Website restrictions"**
4. Нажмите **"+ Add"**
5. Добавьте домен, который вы нашли в шаге 1
6. **Важно:** Используйте правильный формат:
   - `flip-trip.com/*` (для всех страниц домена)
   - `*.fliptripfrontend.vercel.app/*` (для всех поддоменов)
   - `fliptripfrontend.vercel.app/*` (для конкретного поддомена)
7. Нажмите **"SAVE"**

### 3. Проверить API restrictions

Убедитесь, что включены:
- ✅ Maps JavaScript API
- ✅ Places API
- ✅ Places API (New)
- ✅ Geocoding API

### 4. Проверить результат

1. Подождите 1-2 минуты
2. Обновите страницу
3. Фотографии должны загрузиться
4. В консоли не должно быть ошибок 403

## Частые проблемы

### Проблема: Домен добавлен, но все равно 403
**Решение:** 
- Проверьте формат домена (должен заканчиваться на `/*`)
- Убедитесь, что домен точно совпадает с Referer в запросе
- Попробуйте добавить `*.vercel.app/*` для всех preview deployments

### Проблема: Фото работают локально, но не на продакшене
**Решение:**
- Добавьте домен продакшена в Application Restrictions
- Проверьте, что домен правильно указан (с `https://` или без)

### Проблема: Фото работают на одном домене, но не на другом
**Решение:**
- Добавьте все используемые домены в Application Restrictions
- Используйте wildcard `*.domain.com/*` для поддоменов

