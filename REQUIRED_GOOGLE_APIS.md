# 📋 Список необходимых Google Maps API

## Обязательные API для работы приложения

В разделе **"API restrictions"** → **"Restrict key"** должны быть включены следующие API:

### Основные API (обязательно):
1. ✅ **Maps JavaScript API** - для отображения карт на фронтенде
2. ✅ **Places API** - для поиска мест и получения информации о локациях
3. ✅ **Places API (New)** - для загрузки фотографий локаций
4. ✅ **Geocoding API** - для преобразования адресов в координаты

### Дополнительные API (могут использоваться):
5. ✅ **Maps Static API** - для статических карт (если используется)
6. ✅ **Directions API** - для построения маршрутов (если используется)
7. ✅ **Distance Matrix API** - для расчета расстояний (если используется)

## Что НЕ нужно включать

Эти API можно НЕ включать, если они не используются:
- ❌ Maps SDK for Android (только для мобильных приложений)
- ❌ Maps SDK for iOS (только для мобильных приложений)
- ❌ Street View Static API (если не используется Street View)
- ❌ Time Zone API (если не используется)
- ❌ Roads API (если не используется)
- ❌ И другие специфические API

## Минимальный набор для работы

Для базовой работы достаточно:
1. Maps JavaScript API
2. Places API
3. Places API (New)
4. Geocoding API

## Как проверить

1. Google Cloud Console → **APIs & Services** → **Credentials**
2. Откройте ваш API ключ
3. В разделе **"API restrictions"** выберите **"Restrict key"**
4. Убедитесь, что включены минимум 4 API выше
5. Сохраните

