# 🔧 Решение проблем с Google Maps API

## ❌ Ошибка: "Google Maps API key is not configured"

### Проблема 1: Ключ в неправильном проекте Vercel

**Симптомы:**
- Ключ добавлен в `fliptripback` (backend)
- Но нужен в `fliptripfrontend` (frontend)

**Решение:**
1. Откройте проект **`fliptripfrontend`** на Vercel (НЕ `fliptripback`)
2. Settings → Environment Variables
3. Добавьте `VITE_GOOGLE_MAPS_KEY` с вашим ключом
4. Выберите все окружения
5. Перезапустите деплой

### Проблема 2: Не включен Maps JavaScript API

**Симптомы:**
- Включен только Places API
- Но нужен Maps JavaScript API

**Решение:**
1. Google Cloud Console → APIs & Services → Library
2. Найдите **"Maps JavaScript API"**
3. Нажмите **"ENABLE"**
4. Подождите 1-2 минуты

### Проблема 3: Неправильные ограничения ключа

**Симптомы:**
- Ключ работает локально, но не на продакшене
- Ошибка `RefererNotAllowedMapError`

**Решение:**
1. Google Cloud Console → APIs & Services → Credentials
2. Откройте ваш ключ
3. Application restrictions → HTTP referrers
4. Добавьте домены:
   ```
   flip-trip.com/*
   www.flip-trip.com/*
   *.fliptripfrontend.vercel.app/*
   localhost:5173/*
   ```
5. Сохраните

### Проблема 4: Деплой не перезапущен

**Симптомы:**
- Ключ добавлен, но ошибка остается
- Переменная не видна в приложении

**Решение:**
1. Vercel → Deployments
2. Найдите последний деплой
3. Три точки (⋮) → **Redeploy**
4. Подождите завершения деплоя
5. Обновите страницу

## ✅ Чеклист проверки

- [ ] Ключ добавлен в проект **`fliptripfrontend`** (frontend)
- [ ] Имя переменной: `VITE_GOOGLE_MAPS_KEY` (с префиксом `VITE_`)
- [ ] Выбраны все окружения (Production, Preview, Development)
- [ ] Включен **Maps JavaScript API** (не только Places API)
- [ ] Включен **Places API**
- [ ] Включен **Geocoding API** (рекомендуется)
- [ ] Деплой перезапущен после добавления переменной
- [ ] Ограничения ключа настроены правильно
- [ ] Домены добавлены в HTTP referrers

## 🔍 Как проверить, что ключ работает

1. Откройте консоль браузера (F12)
2. Должны увидеть:
   ```
   🗺️ Loading Google Maps API with key: AIzaSy...
   ✅ Google Maps script loaded successfully
   ```
3. Если видите ошибку `InvalidKeyMapError`:
   - Проверьте, что ключ правильный
   - Проверьте, что Maps JavaScript API включен
   - Проверьте ограничения ключа

## 📞 Если ничего не помогает

1. Проверьте квоты в Google Cloud Console:
   - APIs & Services → Quotas
   - Убедитесь, что не превышены лимиты

2. Проверьте биллинг:
   - Убедитесь, что аккаунт активен
   - Google Maps API имеет бесплатный лимит $200/месяц

3. Создайте новый ключ:
   - Иногда проще создать новый ключ
   - Следуйте инструкции заново

