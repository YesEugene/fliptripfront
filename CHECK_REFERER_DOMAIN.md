# 🔍 Проверка домена в Referer для исправления 403 ошибок

## Проблема
Все домены добавлены правильно, но фото все еще не загружаются (403 ошибки).

## Решение: Проверить точный Referer

### Шаг 1: Найти точный Referer в запросе

1. Откройте консоль браузера (F12) → вкладка **Network**
2. Найдите запрос к `/place/photo` с ошибкой 403
3. Откройте его → вкладка **Headers**
4. Найдите раздел **Request Headers**
5. Найдите строку **Referer** - скопируйте **ТОЧНОЕ** значение

**Примеры Referer, которые могут быть:**
- `https://www.flip-trip.com/`
- `https://www.flip-trip.com/guide/tours/visualizer/...`
- `https://flip-trip.com/` (без www)
- `https://flip-trip.com/itinerary?...`
- `https://fliptripfrontend.vercel.app/...`
- `https://fliptripfrontend-xxx.vercel.app/...` (preview deployment)

### Шаг 2: Добавить все возможные варианты

В Google Cloud Console → **APIs & Services** → **Credentials** → ваш ключ → **Website restrictions**, добавьте **ВСЕ** эти варианты:

#### Основные домены:
1. `flip-trip.com/*`
2. `www.flip-trip.com/*`
3. `*.flip-trip.com/*` (для всех поддоменов)

#### Vercel deployments:
4. `fliptripfrontend.vercel.app/*`
5. `*.fliptripfrontend.vercel.app/*` (для preview deployments)
6. `*.vercel.app/*` (для всех Vercel проектов)

#### Локальная разработка:
7. `localhost:5173/*`
8. `localhost:3000/*` (если используется)

### Шаг 3: Проверить, нет ли других доменов

Если Referer показывает другой домен (например, preview deployment), добавьте его тоже.

### Шаг 4: Важные моменты

1. **Формат:** БЕЗ `https://`, С `/*` в конце
2. **Оба варианта:** С www и без www
3. **Wildcard:** Используйте `*.domain.com/*` для всех поддоменов
4. **Сохранить:** После добавления нажмите **SAVE**
5. **Подождать:** Изменения применяются 2-5 минут
6. **Hard Refresh:** После ожидания сделайте Cmd+Shift+R

### Шаг 5: Если все еще не работает

Попробуйте временно:
1. В **Application restrictions** выберите **"None"**
2. Сохраните
3. Обновите страницу
4. Если фото загружаются - значит проблема точно в доменах
5. Вернитесь к **"Websites"** и добавьте домены заново, проверяя каждый шаг

## Альтернативное решение

Если ограничения по доменам не работают, можно использовать прокси на бэкенде для загрузки фото через серверный ключ (без ограничений по доменам).

