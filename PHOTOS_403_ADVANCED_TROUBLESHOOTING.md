# 🔍 Расширенная диагностика 403 ошибок для фотографий

## Если домены добавлены правильно, но все равно 403

### 1. Проверить Places API (New) в API Restrictions

**КРИТИЧЕСКИ ВАЖНО:** Для фотографий нужен именно **Places API (New)**, а не просто Places API.

1. Google Cloud Console → **APIs & Services** → **Credentials**
2. Откройте ваш API ключ
3. В разделе **"API restrictions"**:
   - Убедитесь, что выбрано **"Restrict key"**
   - Нажмите на выпадающий список (показывает "31 APIs")
   - Найдите и убедитесь, что **"Places API (New)"** отмечен галочкой
   - Если его нет - добавьте его
4. Нажмите **"SAVE"**

### 2. Проверить, что Places API (New) включен

1. Google Cloud Console → **APIs & Services** → **Library**
2. Найдите **"Places API (New)"**
3. Убедитесь, что статус **"API Enabled"** с зеленой галочкой
4. Если не включен - нажмите **"ENABLE"**

### 3. Очистить кеш браузера

Иногда браузер кеширует старые ответы 403:

1. Откройте консоль браузера (F12)
2. Перейдите на вкладку **Network**
3. Установите галочку **"Disable cache"**
4. Или используйте **Hard Refresh**: 
   - Windows/Linux: `Ctrl + Shift + R`
   - Mac: `Cmd + Shift + R`

### 4. Проверить точный Referer в запросе

1. Откройте консоль браузера (F12) → **Network**
2. Найдите запрос к `/place/photo` с ошибкой 403
3. Откройте его → вкладка **Headers**
4. Найдите **Request Headers** → **Referer**
5. Скопируйте точное значение (например, `https://www.flip-trip.com/` или `https://www.flip-trip.com/itinerary?...`)
6. В Google Cloud Console добавьте домен **точно так, как он указан в Referer**, но:
   - Уберите `https://`
   - Добавьте `/*` в конце
   - Например: `www.flip-trip.com/*`

### 5. Попробовать временно убрать ограничения

**ТОЛЬКО ДЛЯ ТЕСТИРОВАНИЯ:**

1. В **Application restrictions** выберите **"None"** (временно)
2. Сохраните
3. Обновите страницу
4. Если фото загружаются - значит проблема точно в Application Restrictions
5. Вернитесь к **"Websites"** и добавьте домены заново

### 6. Проверить, нет ли конфликта с другими ключами

1. Убедитесь, что используется правильный API ключ
2. Проверьте, что в коде используется ключ из `VITE_GOOGLE_MAPS_KEY`
3. Убедитесь, что этот ключ добавлен в Vercel

### 7. Проверить квоты и биллинг

1. Google Cloud Console → **APIs & Services** → **Quotas**
2. Проверьте, не превышены ли квоты для Places API (New)
3. Проверьте биллинг - убедитесь, что аккаунт активен

### 8. Попробовать другой формат домена

Иногда Google требует разные форматы:

Попробуйте добавить все варианты:
- `www.flip-trip.com/*`
- `flip-trip.com/*`
- `*.flip-trip.com/*`
- `https://www.flip-trip.com/*` (хотя обычно не нужен)
- `https://flip-trip.com/*` (хотя обычно не нужен)

### 9. Проверить время применения изменений

Изменения в Google Cloud Console могут применяться с задержкой:
- Обычно: 1-2 минуты
- Иногда: до 5-10 минут
- После изменений: подождите и сделайте Hard Refresh (Ctrl+Shift+R)

### 10. Проверить в режиме инкогнито

1. Откройте страницу в режиме инкогнито
2. Проверьте, загружаются ли фото
3. Это поможет исключить проблемы с кешем или расширениями браузера

## Если ничего не помогает

### Альтернативное решение: Прокси через бэкенд

Если ограничения по доменам не работают, можно загружать фото через бэкенд:

1. Создать endpoint на бэкенде: `/api/place-photo-proxy`
2. Фронтенд запрашивает фото через этот endpoint
3. Бэкенд использует серверный ключ (без ограничений по доменам)
4. Бэкенд возвращает фото фронтенду

Это обходит проблему с Application Restrictions, но требует дополнительной разработки.

