# Настройка Google Maps API для блока карты

## Проблема
Если вы видите ошибку `InvalidKeyMapError` или сообщение "Ошибка загрузки карты", это означает, что Google Maps API ключ не настроен или неверный.

## Решение

### 1. Получите Google Maps API ключ

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий
3. Перейдите в **APIs & Services** → **Credentials**
4. Нажмите **Create Credentials** → **API Key**
5. Скопируйте созданный ключ

### 2. Включите необходимые API

В Google Cloud Console включите следующие API:
- **Maps JavaScript API** (обязательно)
- **Places API** (для поиска мест)
- **Geocoding API** (для преобразования адресов в координаты)

### 3. Настройте ограничения ключа (рекомендуется)

1. Откройте созданный API ключ
2. В разделе **Application restrictions** выберите **HTTP referrers (web sites)**
3. Добавьте домены:
   - `flip-trip.com/*`
   - `www.flip-trip.com/*`
   - `fliptripfrontend.vercel.app/*`
   - `localhost:5173/*` (для разработки)
4. В разделе **API restrictions** выберите **Restrict key**
5. Выберите только необходимые API (Maps JavaScript API, Places API, Geocoding API)
6. Сохраните изменения

### 4. Добавьте ключ в Vercel

1. Откройте проект на [Vercel](https://vercel.com/)
2. Перейдите в **Settings** → **Environment Variables**
3. Добавьте новую переменную:
   - **Name:** `VITE_GOOGLE_MAPS_KEY`
   - **Value:** ваш API ключ
   - **Environment:** Production, Preview, Development (выберите все)
4. Сохраните изменения

### 5. Перезапустите деплой

После добавления переменной окружения:
1. Перейдите в **Deployments**
2. Найдите последний деплой
3. Нажмите **Redeploy** (или дождитесь автоматического перезапуска)

### 6. Проверка

После перезапуска:
1. Обновите страницу визуализатора
2. Блок карты должен загрузиться без ошибок
3. Проверьте консоль браузера (F12) - не должно быть ошибок `InvalidKeyMapError`

## Важные замечания

- ⚠️ **Не коммитьте API ключ в Git!** Ключ должен быть только в переменных окружения Vercel
- 🔒 **Ограничьте ключ** по доменам и API для безопасности
- 💰 **Следите за квотами** - Google Maps API имеет лимиты бесплатного использования
- 🔄 **После изменения ключа** может потребоваться несколько минут для обновления

## Проверка текущей конфигурации

В консоли браузера вы должны увидеть:
```
🗺️ Loading Google Maps API with key: AIzaSy...
✅ Google Maps script loaded successfully
```

Если видите ошибку, проверьте:
1. Правильность имени переменной: `VITE_GOOGLE_MAPS_KEY` (с префиксом `VITE_`)
2. Что переменная добавлена во все окружения (Production, Preview, Development)
3. Что деплой был перезапущен после добавления переменной




