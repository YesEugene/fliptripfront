# 🔧 Исправление проблемы с фотографиями локаций

## Проблема
После добавления ограничений (restrictions) к API ключу исчезли изображения в блоках локаций. В консоли появляются ошибки `403 (Forbidden)` для запросов к `maps.googleapis.com/maps/api/place/photo`.

## Причина
Для загрузки фотографий из Google Places API требуется **Places API (New)**, а не только старый Places API.

## Решение

### 1. Включить Places API (New)

1. Откройте **Google Cloud Console**: https://console.cloud.google.com/
2. Перейдите: **APIs & Services** → **Library**
3. Найдите **"Places API (New)"** (не просто "Places API")
4. Нажмите **"ENABLE"**
5. Подождите 1-2 минуты

### 2. Добавить Places API (New) в ограничения ключа

1. Перейдите: **APIs & Services** → **Credentials**
2. Откройте ваш API ключ
3. В разделе **"API restrictions"**:
   - Убедитесь, что выбрано **"Restrict key"**
   - Найдите и выберите **"Places API (New)"** в списке
   - Также должны быть включены:
     - ✅ Maps JavaScript API
     - ✅ Places API (старый, для совместимости)
     - ✅ Places API (New) ← **это важно для фото**
     - ✅ Geocoding API
4. Нажмите **"SAVE"**

### 3. Проверить Application Restrictions

Убедитесь, что в разделе **"Application restrictions"** добавлены все нужные домены:
- `flip-trip.com/*`
- `www.flip-trip.com/*`
- `*.fliptripfrontend.vercel.app/*`
- `localhost:5173/*`

## После изменений

1. Подождите 1-2 минуты для применения изменений
2. Обновите страницу визуализатора
3. Фотографии должны появиться в блоках локаций
4. Проверьте консоль браузера - не должно быть ошибок `403` для `/place/photo`

## Важно

- **Places API (New)** - это новый API от Google, который заменяет старый Places API
- Для фото требуется именно **Places API (New)**
- Старый **Places API** можно оставить для совместимости, но он не поддерживает фото

